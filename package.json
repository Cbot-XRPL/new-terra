{
  "name": "new-terra",
  "private": true,
  "version": "0.1.0",
  "description": "New Terra Construction — React frontend + Express backend with role-based portals",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel db:start dev:server dev:client",
    "dev:client": "npm --workspace client run dev",
    "dev:server": "wait-on -t 60000 tcp:127.0.0.1:5432 && npm --workspace server run dev",
    "build": "npm --workspace server run build && npm --workspace client run build",
    "test": "npm --workspace server test && npm --workspace client test",
    "start": "npm --workspace server run start",
    "db:setup": "npm --workspace server run db:setup",
    "db:start": "npm --workspace server run db:start",
    "db:migrate": "npm --workspace server run db:migrate",
    "db:seed": "npm --workspace server run db:seed",
    "db:seed:demo": "npm --workspace server run db:seed:demo",
    "db:studio": "npm --workspace server run db:studio"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "wait-on": "^8.0.1"
  }
}
